import { useState, useEffect } from "react";

const {{  name }}  = state => {
    const [isReady, setIsReady] = useState(false);

    return {
        isReady
    };
};

export default {{  name }} ;
